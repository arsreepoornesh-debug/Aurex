# Database connection string (SQLite for local zero-config, or postgresql://... for Supabase / PostgreSQL in prod)
DATABASE_URL="file:./dev.db"

# NextAuth configuration
NEXTAUTH_SECRET="aurex-clinical-exercise-super-secret-key-2026"
NEXTAUTH_URL="http://localhost:3000"

# Resend API for transactional notifications (Optional in dev, mock logger handles emails if empty)
RESEND_API_KEY=""
RESEND_FROM_EMAIL="AUREX Clinical Fitness <notifications@aurex.com>"
